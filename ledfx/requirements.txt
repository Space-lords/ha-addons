# The LedFX engine, pinned to an upstream commit past the 2.1.9 release.
# This commit contains the later Sendspin/audio fixes.
# Pinned to a SHA for reproducibility; bump it to move forward.
#
# Upstream may still self-report version 2.1.9 on this commit; confirm the
# actual build via /api/info and github_sha.
pkgconfig>=1.5.5
setuptools>=69.0.3
wheel>=0.42.0
aiosendspin>=6.0.5,<7.0.0
ledfx @ git+https://github.com/LedFx/LedFx.git@284ec23
